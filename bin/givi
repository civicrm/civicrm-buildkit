#!/usr/bin/env bash

## "givi" and "forkify" have been merged because they're conceptually very similar.

## However, the "givi" commands are easier to type... We should maybe rename "forkify=>givi",
## but it's a bit more convenient to put the stub here.

exec forkify "$@"
